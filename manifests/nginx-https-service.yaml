apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-alaje-notification
spec:
  rules:
    - host: api.notification.alajecollection.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: alaje-notification
                port:
                  number: 8080
  ingressClassName: nginx
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-alaje-notification
  annotations:
   cert-manager.io/issuer: letsencrypt-nginx
spec:
  tls:
   - hosts:
     - api.notification.alajecollection.com
     secretName: letsencrypt-nginx-notification
  rules:
    - host: api.notification.alajecollection.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: alaje-notification
                port:
                  number: 443
  ingressClassName: nginx
