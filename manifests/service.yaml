apiVersion: v1
kind: Service
metadata:
  labels:
    app: alaje-notification
  name: alaje-notification
spec:
  ports:
  - name: http
    port: 443
    protocol: TCP
    targetPort: 8087
  selector:
    app: alaje-notification
